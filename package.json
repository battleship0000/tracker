{
  "name": "daily-routine-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "A privacy-first, installable daily routine tracker.",
  "scripts": {
    "test": "node tests/logic.test.js",
    "check": "node --check app.js && node --check logic.js && node --check sw.js"
  }
}
